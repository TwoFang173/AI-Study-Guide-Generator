# AI Study Guide Generator

A Python script that uses the OpenAI API to turn unstructured class notes into a structured study guide in JSON format.

## How to Run

1.  Clone the repository.
2.  Install dependencies: `pip install openai`
3.  Add your OpenAI API key in the script.
4.  Run the script: `python study_guide.py`
